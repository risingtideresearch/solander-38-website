NEXT_PUBLIC_SANITY_PROJECT_ID=""        # Required — Sanity project ID (sanity.io/manage → Rising Tide Research Foundation)
NEXT_PUBLIC_SANITY_DATASET="production" # Required — Sanity dataset
SANITY_API_READ_TOKEN=""                # Required — read token (Sanity → API → Tokens)
SANITY_DRAFT_SECRET=""                  # Required — shared secret for Draft Mode (/api/draft)
BUTTONDOWN_API_KEY=""                   # Required — Buttondown newsletter API key
NEXT_PUBLIC_PREVIEW_SITE=""            # Set to "true" for the preview Netlify site; omit for production
